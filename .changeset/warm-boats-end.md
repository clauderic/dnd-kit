---
'@dnd-kit/react': minor
---

Re-export `@dnd-kit/abstract`, `@dnd-kit/collision`, and `@dnd-kit/dom` from `@dnd-kit/react` as `abstract`, `collision`, and `dom` namespaces for convenient access to lower-level utilities from the React adapter entry point.
