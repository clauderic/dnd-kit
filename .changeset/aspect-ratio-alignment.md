---
'@dnd-kit/dom': patch
---

Updated the default alignment for the `Feedback` drop animation and the `SortableKeyboardPlugin` to only center align if the aspect ratio of the source and target are similar, otherwise try to align with top left coordinates.
