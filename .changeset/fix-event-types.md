---
'@dnd-kit/react': patch
---

- Update `DragDropEventHandlers` types to be non nullable.
- Export `DragDropEvents` and allow them to be generic
- Allow `DragDropProvider` to be passed `Data` as a generic
