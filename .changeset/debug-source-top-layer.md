---
'@dnd-kit/dom': patch
---

**Debug**: Force the source debug element to be re-promoted to the top of the top layer z-index stack whenever the source element changes, as it can sit underneath the Feedback overlay otherwise.
