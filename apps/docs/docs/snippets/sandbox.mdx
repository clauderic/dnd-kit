export const CodeSandbox = ({ files, height, hero, previewHeight, showTabs, template }) => {
  return (
    <div suppressHydrationWarning={true} className={`not-prose${hero ? ' hero' : ''}`} style={previewHeight ? {'--preview-height': `${previewHeight}px`} : undefined}>
      <code-sandbox
        suppressHydrationWarning={true}
        files={JSON.stringify(files)}
        height={height}
        showTabs={showTabs}
        template={template}
      />
    </div>
  );
};
